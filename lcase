#include <iostream>
#include <cstring>
using namespace std;
int main() {
	char name[10];
	cout<<"Enter your name in upper case: ";
	cin>>name;
	for(int i=0;i<strlen(name);i++) {
    	name[i]=tolower(name[i]);
	}
    	cout<<"Name in Lower Case :"<<name;

	return 0;
}
