#include <iostream>
#include <cstring>
using namespace std;
int main() {
	char name[10];
	cout<<"Enter your name in lower case: ";
	cin>>name;
	for(int i=0;i<strlen(name);i++) {
    	name[i]=toupper(name[i]);
	}
    	cout<<"Name in Upper Case :"<<name;
	
	return 0;
}
